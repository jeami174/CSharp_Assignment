<UserControl x:Class="Presentation_WPF_MainApplication.Views.ContactDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation_WPF_MainApplication.Views"
             xmlns:viewmodels="clr-namespace:Presentation_WPF_MainApplication.ViewModels"
             xmlns:models="clr-namespace:Business.Models;assembly=Business"
             d:DataContext="{d:DesignInstance Type=viewmodels:ContactDetailViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="600" d:DesignWidth="300">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Contact Details" FontSize="20" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Margin="0 20 0 0">
            <TextBlock Text="First name"/>
            <TextBlock Text="{Binding Contact.FirstName}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Last name"/>
            <TextBlock Text="{Binding Contact.LastName}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Email"/>
            <TextBlock Text="{Binding Contact.Email}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Phone"/>
            <TextBlock Text="{Binding Contact.Phone}" Margin="0,0,0,10" Padding="4" />
                
            <TextBlock Text="Street address"/>
            <TextBlock Text="{Binding Contact.StreetAddress}" Margin="0,0,0,10" Padding="4" />
                
            <TextBlock Text="Postal code"/>
            <TextBlock Text="{Binding Contact.PostalCode}" Margin="0,0,0,10" Padding="4" />
                
            <TextBlock Text="City"/>
            <TextBlock Text="{Binding Contact.City}" Margin="0,0,0,10" Padding="4" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button Command="{Binding GoToEditViewCommand}" Content= "Edit" Margin="0 10 0 5" Padding="5" />
            <Button Command="{Binding DeleteCommand}" Content= "Delete" Margin="0 5 0 10" Padding="5" />
            <Button Command="{Binding GoToListViewCommand}" Content= "Go Back" Margin="0 5 0 10" Padding="5" />
        </StackPanel>
        
    </Grid>
</UserControl>
